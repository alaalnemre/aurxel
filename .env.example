# ============================================
# JordanMarket Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values

# Supabase Configuration (REQUIRED)
# Get these from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase service role key (REQUIRED for server-side operations)
# WARNING: Never expose this in client-side code
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Site URL Configuration (REQUIRED for auth redirects)
# Local development: http://localhost:3000
# Production: https://your-domain.vercel.app
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Auth Redirect URL (REQUIRED for OAuth callbacks)
# Must match Supabase Dashboard > Auth > URL Configuration
NEXT_PUBLIC_AUTH_REDIRECT_URL=http://localhost:3000/auth/callback

# ============================================
# VERCEL DEPLOYMENT NOTES:
# ============================================
# 1. Add all variables above to Vercel Environment Variables
# 2. Set NEXT_PUBLIC_SITE_URL to your Vercel domain
# 3. Set NEXT_PUBLIC_AUTH_REDIRECT_URL to https://your-domain.vercel.app/auth/callback
# 4. In Supabase Dashboard > Auth > URL Configuration:
#    - Site URL: https://your-domain.vercel.app
#    - Redirect URLs: 
#      - https://your-domain.vercel.app/**
#      - https://*.vercel.app/**
#      - http://localhost:3000/**
