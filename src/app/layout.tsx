import './globals.css';

export const metadata = {
  title: 'JordanMarket',
  description: "Jordan's Premier Online Marketplace",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
